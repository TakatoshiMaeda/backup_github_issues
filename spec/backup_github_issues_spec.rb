require "spec_helper"

describe BackupGithubIssues do
  it "has a version number" do
    expect(BackupGithubIssues::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
